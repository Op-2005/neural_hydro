# Local-Subgraph Loss-Distribution Invariant

Tracked metric: per-seed median NSE, reported as **mean ± std across 3 seeds**.
Stable across runs by design; this is the quantity we watch as we revise model/data.
