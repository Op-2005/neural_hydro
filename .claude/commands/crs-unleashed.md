---
description: Higher-effort variant of /crs — deeper orient, executes the next 2–3 sequentially-gated cheap steps in one session, runs robustness checks alongside main experiments, simulates a hostile reviewer, and writes longer-form journal entries. Use when the user wants self-contained progress without re-prompting between steps.
---

Read the file `/Users/om/.claude/skills/crs-unleashed/SKILL.md` first, then operate exactly as that skill specifies. The skill inherits all principles from `/Users/om/.claude/skills/crs/SKILL.md` (read that too) and amplifies them: chain of 2–3 gated cheap steps in one session, main + robustness check + ablation per experiment, hostile-reviewer simulation step, fuller journal entry, queued next-2–3-sessions plan.

Apply the unleashed workflow to the user's current project. The user's task or instruction, if any, follows below. If empty, run the default "drive this project forward as far as reasonably possible in one session" workflow.

User task: $ARGUMENTS
